def process_data():
    # 1. Проверка и подготовка
    if not os.path.exists(input_file):
        print(f"❌ Ошибка: исходный файл не найден: {input_file}")
        return

    os.makedirs(output_folder, exist_ok=True)
    print(f"🔹 Папка для результатов: {output_folder}")

    # 2. Загрузка исходных данных
    source_df = pd.read_excel(input_file)
    print(f"✅ Загружен исходный файл. Записей: {len(source_df)}")

    if 'G33 (код товара по ТН ВЭД РФ)' not in source_df.columns:
        print("❌ В файле отсутствует колонка 'G33 (код товара по ТН ВЭД РФ)'")
        return

    # 3. Создание словаря соответствий "ТН ВЭД" -> "Продукт"
    product_mapping = {
        "2834210000": "Калиевая селитра",
        "2915120000": "Формиаты",
        "3102600000": "Нитрат кальция",
        "3102900000": "Нитрат кальция",
        "3105902000": "Нитрат кальция",
        "2834298000": "Нитрат кальция",
        "3102500000": "Нитрит натрия",
        "2834100000": "Нитрит натрия",
        "3105400000": "МАФ",
        "3105100000": "NPK",
        "3105200000": "NPK",
        "3105201000": "NPK",
        "3105209000": "NPK",
        "3105510000": "NPK",
        "3105590000": "NPK",
        "3105600000": "NPK",
        "3105908000": "NPK",
        "3105909100": "NPK",
        "3105909900": "NPK",
        "2833210000": "Сульфат магния",
        "2835240000": "Монокалийфосфат",
        "3105100": "Экспорт NPK",
        "3105200": "Экспорт NPK",
        "3105201": "Экспорт NPK",
        "3105209": "Экспорт NPK",
        "3105908": "Экспорт NPK",
        "3105400001": "Экспорт МАФ"
    }

    # Приводим колонку к строковому типу и применяем маппинг
    source_df['G33 (код товара по ТН ВЭД РФ)'] = source_df['G33 (код товара по ТН ВЭД РФ)'].astype(str)
    source_df['Продукт'] = source_df['G33 (код товара по ТН ВЭД РФ)'].map(product_mapping)

    # 4. Сохранение результата в один файл
    output_path = os.path.join(output_folder, "Обработанные_данные.xlsx")
    source_df.to_excel(output_path, index=False)
    print(f"\n💾 Результат сохранён в: {output_path}")
    print(f"📊 Общее количество записей: {len(source_df)}")
    print(f"🆕 Добавлена колонка: 'Продукт'")
    print("\n✅ Обработка завершена!")
